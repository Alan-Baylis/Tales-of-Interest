div(ng-class="{hidden: !showHistory}").panel.panel-default.history-panel
  .panel-body
    .page-header
      h1 History {{t}}

    ul.list-group
      li(ng-repeat="item in historyItems", ng-click="$parent.link(item.path)").list-group-item
        | {{item.subject}}